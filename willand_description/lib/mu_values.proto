syntax = "proto3";

package willand_description;

// 定义一个消息类型来存储四个 mu 值
message MuValues {
    double mu_sim_l = 1;  // 左后轮的 mu
    double mu_sim_r = 2;  // 右后轮的 mu
    double mu_model_l = 3;  // 左后轮模型的 mu
    double mu_model_r = 4;  // 右后轮模型的 mu
}
